services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: workout_service
      POSTGRES_USER: workout
      POSTGRES_PASSWORD: workout
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
