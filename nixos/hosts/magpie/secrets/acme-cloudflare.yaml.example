# Copy to acme-cloudflare.yaml and encrypt with `sops nixos/hosts/magpie/secrets/acme-cloudflare.yaml`.
# The Cloudflare token is shared across the lab; lift the value from another
# host's acme-cloudflare.yaml rather than minting a new one.
cloudflare_dns_api_token: replace-me
